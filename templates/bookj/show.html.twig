{% extends 'base.html.twig' %}

{% block title %}Bookj{% endblock %}

{% block body %}
    <h1>Bookj</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bookj.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ bookj.titre }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ bookj.annee }}</td>
            </tr>
            <tr>
                <th>Resumee</th>
                <td>{{ bookj.resumee }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ bookj.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bookj_index') }}">back to list</a>

    <a href="{{ path('app_bookj_edit', {'id': bookj.id}) }}">edit</a>

    {{ include('bookj/_delete_form.html.twig') }}
{% endblock %}
