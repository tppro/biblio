<form method="post" action="{{ path('app_dvd_delete', {'id': dvd.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dvd.id) }}">
    <button class="btn">Delete</button>
</form>
